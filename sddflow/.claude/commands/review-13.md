# /review-13 — 화면-API 매핑 검증 (별도 세션에서 실행)

## 역할
너는 QA Reviewer다. 수정하지 않는다. 보고만 한다.

## 읽을 파일
1. specs/09_화면_상세_명세서.md (기준 1)
2. specs/11_API_스펙.md (기준 2)
3. specs/13_화면_API_매핑.md (검증 대상)

모두 처음부터 읽어라.

## 검증 항목
1. 09의 모든 화면의 모든 사용자 행위에 API가 매핑되었는가 (프론트 전용 행위 제외)
2. 매핑된 API가 11_API_스펙에 실제로 존재하는가
3. 11_API_스펙의 모든 엔드포인트가 최소 1개 화면에 매핑되었는가
4. Mock 데이터가 3종(성공/빈값/에러) 모두 정의되었는가
5. Mock 데이터의 응답 필드가 11_API_스펙의 응답 형식과 일치하는가
6. API 요청 파라미터가 11_API_스펙의 요청 형식과 일치하는가

## 결과 저장
specs/_reviews/13_review.md

## 출력 형식
```markdown
# 리뷰: 13_화면_API_매핑.md
- 일시: {타임스탬프}
- 기준: 09_화면_상세_명세서.md, 11_API_스펙.md

## 행위 → API 매핑
| 화면 | 행위 | 매핑 API | API 스펙 존재 | 상태 |

## API 스펙 매핑률
| API 엔드포인트 | 매핑 화면 | 상태 |

## Mock 데이터 검증
| API | 성공 | 빈값 | 에러 | 응답 형식 일치 |

## 수치
- 행위 → API 매핑률: {N}/{M} ({%})
- API 스펙 매핑률: {N}/{M} ({%})
- Mock 3종 완비율: {N}/{M} ({%})

## 판단
✅ 다음 진행 가능 / ❌ 수정 필요 ({사유})
```

## 절대 규칙
- 검증만 해라. 13_화면_API_매핑을 수정하지 마라.
- 숫자로 보여줘라.
