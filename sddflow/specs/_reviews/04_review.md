# 리뷰: 04_도메인_분리.md
- 일시: 2026-04-20
- 기준: 03_ERD.md

## 테이블 배정
| 테이블 | 배정 모듈 | 상태 |
|--------|----------|------|
| user | 사용자/인증 | ✅ 소유 (다른 모듈은 owner_id 읽기만) |
| schedule | 일정 | ✅ 소유 |
| memo | 메모 | ✅ 소유 |
| schedule_memo_link | 연결 | ✅ 소유 (02의 "붙이기/해제" 행위 귀속) |
| tag | 태그 | ✅ 소유 |
| schedule_tag | 태그 | ✅ 소유 (02 "일정에 태그 붙이기" 행위 귀속) |
| memo_tag | 태그 | ✅ 소유 (02 "메모에 태그 붙이기" 행위 귀속) |
| attachment | 첨부 | ✅ 소유 (target_type + target_id 다형) |

### 배정 안 된 테이블 목록
- 없음 (8/8)

### 다중 소유 검사
- 모든 테이블이 **단 하나의 모듈**에만 소유 배정됨. 다른 모듈은 공통 인터페이스(`ScheduleReader`, `MemoReader`, `TagReader`, `LinkReader`)를 통한 **읽기 접근**만 명시됨 → 헌법 §모듈 격리 원칙 준수.
- 소유자 없는 도메인(검색·알림)은 소유 테이블 0개로 명시되어 있으며 ERD 근거 일치(03 §검색/§알림 "별도 테이블 없음").

## 양방향 의존
| 모듈 A → 모듈 B | 모듈 B → 모듈 A | 순환? |
|---------------|---------------|-------|
| 연결 → 일정 (`ScheduleReader`) | 일정 → 연결 | ❌ 없음 (일정은 `ResourceDeletedEvent` 공통 모듈로 발행, 구독자 모름) |
| 연결 → 메모 (`MemoReader`) | 메모 → 연결 | ❌ 없음 (동일 패턴) |
| 태그 → 일정 (`ScheduleReader`) | 일정 → 태그 | ❌ 없음 ("태그 부여" 행위가 태그 모듈 귀속 — 02 근거) |
| 태그 → 메모 (`MemoReader`) | 메모 → 태그 | ❌ 없음 (동일 패턴) |
| 첨부 → 일정 (`ScheduleReader`) | 일정 → 첨부 | ❌ 없음 (일정은 이벤트 발행만, 첨부가 구독) |
| 첨부 → 메모 (`MemoReader`) | 메모 → 첨부 | ❌ 없음 (동일 패턴) |
| 검색 → 일정/메모/태그 | — | ❌ 없음 (검색은 순수 소비자) |
| 알림 → 일정/연결/메모 | — | ❌ 없음 (알림은 순수 소비자, 이벤트는 구독만) |
| 모든 모듈 → 사용자/인증 (`CurrentUser`) | 사용자/인증 → 그 외 | ❌ 없음 (사용자/인증은 루트, 의존 없음) |

- 02의 양방향 의존(일정↔연결, 메모↔연결, 일정↔태그, 메모↔태그, 일정↔첨부, 메모↔첨부)이 모두 **공통 모듈 인터페이스 + 도메인 이벤트**로 단방향화됨. 06(공통 구조)에서 `ResourceDeletedEvent`, `ScheduleReader/MemoReader/TagReader/LinkReader/CurrentUser`가 실제로 정의되어야 함 — 인수 조건으로 플래그.

## 컨텍스트 크기 검사
| 모듈 | 소유 테이블 | 명세 길이 | 판단 |
|------|-----------|----------|------|
| 사용자/인증 | 1 | 소 | ✅ 단일 윈도우 |
| 일정 | 1 | 중 | ✅ |
| 메모 | 1 | 소~중 | ✅ |
| 연결 | 1 | 소 | ✅ |
| 태그 | 3 | 중 | ✅ |
| 첨부 | 1 | 중 | ✅ |
| 검색 | 0 | 소 | ✅ |
| 알림 | 0 | 중 | ✅ |

- 최대 소유 테이블 3개(태그). 10개 이상 모듈 없음 → "대" 등급 0건.

## 수치
- 배정률: 8/8 (100%)
- 양방향 의존: 0건 (02의 6개 양방향이 모두 공통 모듈 경유 단방향으로 해소됨)
- 테이블 다중 소유: 0건
- "대" 컨텍스트 모듈: 0개

## 판단
✅ 다음 진행 가능

### 경미 메모 (다음 세션에서 해소)
1. **세션 5(공용 인터페이스)에서 반드시 정의할 산출물**
   - 인터페이스: `CurrentUser`, `ScheduleReader`, `MemoReader`, `TagReader`, `LinkReader`
   - 도메인 이벤트: `ResourceDeletedEvent { type: SCHEDULE|MEMO, id, ownerId }`
   - 이 계약이 05에서 누락되면 04의 단방향화 근거가 무너짐 → 05 리뷰 시 교차 확인 필요.
2. **연결 모듈의 양쪽 소유자 동일성 검증** — 04 §연결에서 "양쪽 자산이 동일 소유자인지 검증" 명시됨. 애플리케이션 레벨 검증이므로 /session11·/session12에서 테스트 케이스로 보장되어야 함.
3. **ON DELETE CASCADE vs 앱 레벨 삭제 선택** — 03과 04 모두 미결로 남겨둠. /session14(ERD 최종) 전까지 결정.
4. **"연결" 모듈 이름** — 02_review의 경미 지적(모호한 이름). 04에서도 부제 "일정-메모 링크"만 달아둠. /session16(MODULE_SPEC) 시점에 구체 네이밍(예: `schedule-memo-link`) 확정 필요.
