spring:
  datasource:
    url: jdbc:h2:file:./data/calendar;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: validate

calendar:
  profile: integ
  security:
    permit-all: false
